"""autofree.core — 抽自 AutoTeam-F 的 freegen 模块。

注册 + OAuth + cloud-mail + SMS provider + 本地存储 + CPA push 全套核心。
不依赖 web 层(api/auth/db),只依赖 autofree.settings + autofree.core.config(从 DB Settings 读运行时配置)。
"""
